---
- name: Install nfs-ganesha and rpcbind
  ansible.builtin.apt:
    name:
      - nfs-ganesha
      - nfs-ganesha-vfs
      - rpcbind
    state: present
    update_cache: true

- name: Enable and start rpcbind
  ansible.builtin.systemd:
    name: rpcbind
    enabled: true
    state: started

- name: Create export directory
  ansible.builtin.file:
    path: "{{ nfs_export_path }}"
    state: directory
    owner: nobody
    group: nogroup
    mode: "0777"

- name: Configure ganesha
  ansible.builtin.template:
    src: ganesha.conf.j2
    dest: /etc/ganesha/ganesha.conf
    owner: root
    group: root
    mode: "0644"
  notify: Restart ganesha

- name: Enable and restart ganesha
  ansible.builtin.systemd:
    name: nfs-ganesha
    enabled: true
    state: restarted