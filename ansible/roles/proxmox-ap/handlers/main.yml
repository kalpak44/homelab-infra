---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart hostapd
  ansible.builtin.systemd:
    name: hostapd
    state: restarted

- name: Restart dhcp-relay
  ansible.builtin.systemd:
    name: isc-dhcp-relay
    state: restarted