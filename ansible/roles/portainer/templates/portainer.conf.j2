server {
    listen 80;
    server_name {{ portainer_tls_domain }};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name {{ portainer_tls_domain }};

    ssl_certificate     /etc/letsencrypt/live/{{ portainer_tls_domain }}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/{{ portainer_tls_domain }}/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    location / {
        proxy_pass         http://127.0.0.1:{{ portainer_internal_port }};
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}