apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopify-gpt-assistant
  namespace: public
  labels:
    app: shopify-gpt-assistant
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  minReadySeconds: 10
  selector:
    matchLabels:
      app: shopify-gpt-assistant
  template:
    metadata:
      labels:
        app: shopify-gpt-assistant
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      containers:
        - name: shopify-gpt-assistant
          image: ghcr.io/kalpak44/shopify-gpt-assistant:5567ae5
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
              ephemeral-storage: 512Mi
            limits:
              memory: 512Mi
          env:
            - name: NODE_ENV
              value: development
            - name: OPENAI_BASE_URL
              value: https://api.openai.com/v1
            - name: SHOPIFY_APP_URL
              value: https://shopify-gpt-assistant.pavel-usanli.online
            - name: HOST
              value: 0.0.0.0
            - name: SHOPIFY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: shopify-gpt-assistant-secrets
                  key: SHOPIFY_API_KEY
            - name: SHOPIFY_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: shopify-gpt-assistant-secrets
                  key: SHOPIFY_API_SECRET
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: shopify-gpt-assistant-secrets
                  key: DATABASE_URL
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: shopify-gpt-assistant-secrets
                  key: OPENAI_API_KEY

