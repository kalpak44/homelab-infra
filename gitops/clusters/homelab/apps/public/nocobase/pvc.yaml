apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nocobase-storage
  namespace: public
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  resources:
    requests:
      storage: 10Gi