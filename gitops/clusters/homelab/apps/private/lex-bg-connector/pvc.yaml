apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lex-bg-connector-data
  namespace: private
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 10Gi